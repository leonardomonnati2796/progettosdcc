# Registry node 1: primo nodo del cluster Docker.
node:
  id: registry-node-1
  listen_address: 0.0.0.0:50051
  advertise_address: registry-node-1:50051
  storage_dir: /app/data

cluster:
  # Tutti i nodi condividono lo stesso elenco di peer iniziali.
  seed_peers:
    - registry-node-1:50051
    - registry-node-2:50051
    - registry-node-3:50051
  gossip_interval_seconds: 2
  gossip_start_delay_seconds: 0
  gossip_gate_path: /app/data/.gossip-enabled
  reconcile_interval_seconds: 5
  peer_timeout_seconds: 8
  max_gossip_fanout: 2

service:
  # TTL condiviso per rendere omogenea la rilevazione dei servizi inattivi.
  heartbeat_ttl_seconds: 60
